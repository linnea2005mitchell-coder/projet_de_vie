// message philosophique: saute dans une flaque avant que la flaque te saute
#include <vector>

void verif_balls(int& x, int& y, int& r, int& dx, int& dy);

class Ball {
    public : 
    private :
    double centre_x;
    double centre_y;
    double r;
    double dx;
    double dy;

};

vector<Ball> stock; 